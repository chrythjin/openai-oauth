# Codex Token Inventory

**Last updated:** 2026-04-19
**Auth file location:** `~/.codex/` and `~/.codex/vault/`
**Active token file:** `~/.codex/auth.json` (프록시 실행 시 사용되는 파일)

---

## 토큰 전환 명령

| 이메일 | 명령 |
|--------|------|
| dud***@zongym.net | `node -e "require('fs').writeFileSync(require('path').join(process.env.HOME,'.codex','auth.json'), JSON.stringify(JSON.parse(require('fs').readFileSync(require('path').join(process.env.HOME,'.codex','vault','auth-alt1.json'),'utf8')),null,2))"` |
| tha***@thfly.com | `node -e "require('fs').writeFileSync(require('path').join(process.env.HOME,'.codex','auth.json'), JSON.stringify(JSON.parse(require('fs').readFileSync(require('path').join(process.env.HOME,'.codex','auth.json'),'utf8')),null,2))"` (이미 기본값) |
| avi***@*** | `node -e "require('fs').writeFileSync(require('path').join(process.env.HOME,'.codex','auth.json'), JSON.stringify(JSON.parse(require('fs').readFileSync(require('path').join(process.env.HOME,'.codex','vault','auth.json'),'utf8')),null,2))"` |

---

## 인벤토리

| 이메일 | 파일 경로 | access_token 만료 | 상태 |
|--------|----------|-----------------|------|
| dud***@zongym.net | `~/.codex/auth-alt1.json` | 2026-04-28 12:01 | ACTIVE |
| dud***@zongym.net | `~/.codex/vault/auth-alt1.json` | 2026-04-28 12:01 | ACTIVE |
| tha***@thfly.com | `~/.codex/auth.json` | 2026-04-28 18:05 | ACTIVE |
| tha***@thfly.com | `~/.codex/vault/auth-alt2.json` | 2026-04-28 16:46 | ACTIVE |
| avi***@*** | `~/.codex/vault/auth.json` | 2026-04-28 15:48 | ACTIVE |

**참고:** 모든 access_token은 약 10일 후 만료. 만료 후 refresh_token으로 자동 갱신됨.

---

## 파일 구조

```
~/.codex/
├── auth.json          ← 현재 활성 토큰 (프록시 사용)
├── auth-alt1.json     ← dud***@zongym.net
├── vault/
│   ├── auth.json      ← avi***@***
│   ├── auth-alt1.json ← dud***@zongym.net (vault 사본)
│   └── auth-alt2.json ← tha***@*** (vault 사본)
```
