# Vault - 원본 토큰 저장소

이 폴더에는 원본 토큰 파일(.json)이 저장됩니다.

**절대 덮어쓰지 마세요** - 토큰 문제 발생 시 복원용 백업입니다.

## 파일
- `auth.json` - Account 1 원본 (로컬에만 존재)
- `auth-alt1.json` - Account 2 원본 (로컬에만 존재)

## 규칙
1. 원본 토큰은 항상 여기에 보존
2. 토큰 전환 시 active/에서 사용
3. 백업은 backups/에서 관리