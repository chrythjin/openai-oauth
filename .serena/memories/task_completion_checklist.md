After implementation or operational changes: verify modified files with LSP diagnostics when code changed; run relevant tests; run `bun run build`; run `bun run typecheck` noting known unrelated failures if still present; confirm runtime state with exact commands for service/port when proxy operations are involved. Always preserve existing interactive proxy unless explicit cutover is requested. Always write a session report under docs/sessions/YYYYMMDD_HHMMSS_<task>.md.